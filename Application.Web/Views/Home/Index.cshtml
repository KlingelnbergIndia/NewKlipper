@model IEnumerable<UseCaseBoundary.Model.AttendanceRecordDTO>
@{
    ViewData["Title"] = "Home Page";
}

<style>
    th, td {
        text-align: center
    }
</style>
<div>
    <table class="table">
        <thead>
            <tr>
                <th>
                    Date
                </th>
                <th>
                    Time In
                </th>
                <th>
                    Time Out
                </th>
                <th>
                    Late By
                </th>
                <th>
                    OverTime
                </th>
                <th>
                    Total Working Hours
                </th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                var lateByColor = "White";
                var overTimeColor = "White";
                if (item.LateBy._hour != 0 || item.LateBy._minute != 0)
                {
                    lateByColor = "crimson";
                }
                if (item.OverTime._hour != 0 || item.OverTime._minute != 0)
                {
                    overTimeColor = "aquamarine";
                }
                <tr>
                    <td>
                        @item.Date.ToString("D")
                    </td>
                    <td>
                        @item.TimeIn._hour.ToString("D2")
                        :
                        @item.TimeIn._minute.ToString("D2")
                    </td>
                    @if (item.TimeOut._hour == 0 && item.TimeOut._minute == 0)
                    {
                        <td>-</td>
                    }
                    else
                    {
                        <td>
                            @item.TimeOut._hour.ToString("D2")
                            :
                            @item.TimeOut._minute.ToString("D2")
                        </td>
                    }
                    <td style="background:@lateByColor">
                        @item.LateBy._hour.ToString("D2")
                        :
                        @item.LateBy._minute.ToString("D2")
                    </td>
                    <td style="background:@overTimeColor">
                        @item.OverTime._hour.ToString("D2")
                        :
                        @item.OverTime._minute.ToString("D2")
                    </td>
                    <td>
                        @item.WorkingHours._hour.ToString("D2")
                        :
                        @item.WorkingHours._minute.ToString("D2")
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>

